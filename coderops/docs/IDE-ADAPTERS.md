# IDE Adapters

CoderOps generates client-facing adapter artifacts rather than inventing client formats.

## Implemented outputs

- `.github/agents`
- `.github/skills`
- `.agents/skills`
- `coderops/adapters/generated/copilot-mcp.json`
- `coderops/adapters/generated/generic-mcp-profile.json`

## Notes

Client-specific support is documented only where the format is generated and checked into the repository.